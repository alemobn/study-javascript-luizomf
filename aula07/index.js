// string, number, undefined, null, boolean, symbol

const nome = 'Luiz'; // string
const nome1 = "Luiz"; // string
const nome2 = `Luiz`; // string
const num = 10; // number
const num1 = 10.52; // number
let nomeAluno; // undefined = não aponta pra nenhum local na meḿoria
let sobrenomeAluno = null; // nulo = não aponta pra nenhum local na memória
const aprovado = true; // boolean = true or false (lógico)

let a = 2;
const b = a;

console.log(a, b);

a = 3;

console.log(a, b);